<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "The #Codenewbie Dashboard" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div class="contain-to-grid sticky">
      <nav class="top-bar" data-topbar role="navigation" data-options="sticky_on: large">
      <ul class="title-area">
        <li class="name">
          <h1><%= link_to "The #Codenewbie Dashboard", root_path %></h1>
        </li>
        <li class="toggle-topbar menu-icon"><a href="#"><span></span></a></li>
      </ul>
      <section class="top-bar-section">
        <!-- Right Nav Section -->
        <ul class="right">
          <li><%#= link_to "Import questions", questions_import_path %></li>
          <li><%#= link_to "Import answers", answers_import_path %></li>
          <li><%#= link_to "Delete answers", answers_destroy_path, method: :delete %></li>
          <li class="has-dropdown">
            <a href="#">Chat Sessions</a>
            <ul class="dropdown">
              <% find_chatsessions.each do |cs| %>
                <li><%= link_to "Week: "+cs.week, chatsession_path(cs) %></li>
              <% end %>
            </ul>
          </li>
        </ul>
      </section>
      </nav>
    </div>
    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>